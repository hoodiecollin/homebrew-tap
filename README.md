# hoodiecollin/homebrew-tap

Homebrew formulae for my open-source tools.

## Usage

```sh
brew install hoodiecollin/tap/<formula>
```

Formulae here are published automatically by each project's release pipeline (cargo-dist).
